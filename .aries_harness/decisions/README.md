# Decisions Directory

Use this directory for expanded decision records when `../ADR.md` needs deeper per-decision files.

Keep `../ADR.md` as the summary index even if detailed records are added here later.
